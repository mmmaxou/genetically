import {FlipBitMutation} from './FlipBitMutation';
import {NoMutation} from './NoMutation';
import {DynamicalMutation} from './DynamicalMutation';
export const Mutation = {
  FlipBitMutation,
  NoMutation,
  DynamicalMutation,
};
export default Mutation;
